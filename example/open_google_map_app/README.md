# Example App

This is a simple Flutter app with one centered button.
When tapped, it opens Google Maps using `url_launcher`.

## Run

1. Go to the example directory.
2. Install packages.
3. Run the app.

You can run standard Flutter commands from this folder.
